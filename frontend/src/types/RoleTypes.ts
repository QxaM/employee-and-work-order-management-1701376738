/**
 * Represents a role within a system, including its identifier and name.
 * Used for categorizing or defining access levels or responsibilities.
 */
export interface RoleType {
  id: number;
  name: string;
}
