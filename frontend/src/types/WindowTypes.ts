/**
 * Represents dimensions with width and height.
 *
 * Used to represent for example Window size, but can be used for any other width/height size.
 */
export interface Size {
  width: number;
  height: number;
}
