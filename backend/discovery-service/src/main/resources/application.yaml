eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  server:
    renewal-percent-threshold: 0.49
server:
  port: ${PORT:8761}
spring:
  application:
    name: discovery-service
