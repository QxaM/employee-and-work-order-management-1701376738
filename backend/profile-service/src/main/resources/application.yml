server:
  port: ${PORT:8082}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}

spring:
  application:
    name: profile-service

---

spring:
  config:
    activate:
      on-profile: QA
eureka:
  client:
    enabled: false